'use strict';

/**
 * landing-acquisition service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::landing-acquisition.landing-acquisition');
