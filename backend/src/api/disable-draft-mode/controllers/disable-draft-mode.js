'use strict';

/**
 * disable-draft-mode controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::disable-draft-mode.disable-draft-mode');
